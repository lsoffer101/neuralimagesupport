from setuptools import setup

setup(name='neuralimagesupport',
      version='0.0.1',
      author='Lucas Soffer',
      author_email='lucas.soffer1@gmail.com',
      install_requires=[
          "numpy >= 1.20",
        #   "scipy >= 1.6.0",
          "pandas >= 1.3.4",
      ])
    #   entry_points={
    #       'console_scripts': [
    #           'vantage=vantagenis:main',
    #       ],
    #   })
